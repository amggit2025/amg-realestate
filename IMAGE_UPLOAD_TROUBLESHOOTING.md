# 🔍 تشخيص مشكلة رفع الصور إلى Cloudinary

## ❓ المشكلة
المشاريع لا يتم رفعها إلى Cloudinary وبالتالي لا تظهر في الموقع.

## ✅ ما تم التحقق منه

### 1. اتصال Cloudinary
```bash
✅ Cloud Name: dkvqlvucy
✅ API Key: 594228169165472
✅ API Secret: MOhyNfJwsBE9DwQA2s2i4YB8Geg
✅ Test Upload: SUCCESS
```

### 2. ملف البيئة (`.env.local`)
```bash
✅ الملف موجود
✅ NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="dkvqlvucy"
✅ CLOUDINARY_API_KEY="594228169165472"
✅ CLOUDINARY_API_SECRET="MOhyNfJwsBE9DwQA2s2i4YB8Geg"
```

### 3. الكود
```bash
✅ API Route: /api/upload/image - يستخدم signed upload
✅ API Route: /api/admin/projects - يحفظ الصور في قاعدة البيانات
✅ Admin Page: /admin/projects/add - يرسل البيانات بشكل صحيح
```

## 🧪 طرق الاختبار

### الطريقة 1: اختبار HTML التفاعلي
افتح المتصفح على:
```
http://localhost:3000/test-upload.html
```

هذه الصفحة تتيح لك:
- اختيار صورة من جهازك
- رفعها مباشرة إلى `/api/upload/image`
- مشاهدة النتيجة والـ logs التفصيلية

### الطريقة 2: اختبار PowerShell
شغل السكريبت:
```powershell
.\test-upload.ps1
```

هذا السكريبت:
- يتحقق من تشغيل السيرفر
- يحمل صورة تجريبية
- يرفعها إلى الـ API
- يعرض النتيجة بالتفصيل

### الطريقة 3: اختبار من صفحة الأدمن
1. افتح: http://localhost:3000/admin/projects/add
2. املأ البيانات الأساسية
3. اضغط "اختر صورة" في قسم الصور
4. افتح Console في المتصفح (F12)
5. راقب الـ logs

## 🔍 الأسباب المحتملة للمشكلة

### ❌ السبب 1: السيرفر ليس شغال
**الحل:**
```bash
npm run dev
```

### ❌ السبب 2: ملف `.env.local` مفقود
**الحل:**
```bash
Copy-Item .env.local.example .env.local
# ثم املأ البيانات الصحيحة
```

### ❌ السبب 3: متغيرات البيئة غير صحيحة
**الحل:**
1. افتح https://cloudinary.com/console
2. انسخ:
   - Cloud name
   - API Key
   - API Secret (اضغط "Reveal")
3. حدث `.env.local`
4. أعد تشغيل السيرفر

### ❌ السبب 4: حجم الصورة كبير جداً
**الحد الأقصى:** 10 MB

**الحل:**
- ضغط الصورة قبل الرفع
- استخدام أداة مثل TinyPNG

### ❌ السبب 5: نوع الملف غير مدعوم
**الأنواع المدعومة:**
- JPG / JPEG
- PNG
- WebP

**الحل:**
- حول الصورة إلى أحد الأنواع المدعومة

### ❌ السبب 6: مشكلة في CORS
**الحل:**
تحقق من `next.config.ts`:
```typescript
images: {
  remotePatterns: [
    {
      protocol: 'https',
      hostname: 'res.cloudinary.com',
    },
    {
      protocol: 'https',
      hostname: '*.cloudinary.com',
    },
  ],
}
```

## 📊 كيف تعرف أين المشكلة؟

### 1. افتح Browser Console (F12)
عند رفع صورة، ستشاهد logs مثل:
```
📤 Starting upload for: image.jpg image/jpeg 123456
📥 Response status: 200
📥 Response data: {...}
✅ Image added: https://...
```

### 2. تحقق من الـ logs
إذا رأيت:
- ❌ "No file in request" → الملف لم يصل إلى الـ API
- ❌ "Invalid file type" → نوع الملف غير مدعوم
- ❌ "File too large" → حجم الملف كبير جداً
- ❌ "Upload failed" → مشكلة في Cloudinary
- ❌ "Invalid credentials" → بيانات Cloudinary غير صحيحة

## 🎯 الحل الأكيد

### الخطوات:
1. **شغل السيرفر:**
```bash
npm run dev
```

2. **افتح صفحة الاختبار:**
```
http://localhost:3000/test-upload.html
```

3. **اختر صورة صغيرة (أقل من 2 MB):**
- يفضل JPG أو PNG
- أبعاد معقولة (مثل 1920x1080)

4. **راقب النتيجة:**
- إذا نجح الرفع → المشكلة في صفحة الأدمن
- إذا فشل الرفع → المشكلة في الـ API أو Cloudinary

5. **إذا نجح الاختبار، جرب من صفحة الأدمن:**
```
http://localhost:3000/admin/projects/add
```

## 📝 معلومات إضافية

### البيانات الحالية
- **Cloud Name:** dkvqlvucy
- **Upload Folder:** amg-projects
- **API Endpoint:** /api/upload/image
- **Max File Size:** 10 MB
- **Allowed Types:** JPG, PNG, WebP

### تنظيف الصور المرفوعة
إذا رفعت صور تجريبية كثيرة:
1. اذهب إلى https://cloudinary.com/console
2. Media Library
3. افتح مجلد `amg-projects`
4. احذف الصور التجريبية

## ❓ أسئلة مهمة

1. **هل السيرفر يعمل؟**
   - نعم ✅ → تابع
   - لا ❌ → شغله بـ `npm run dev`

2. **هل تظهر رسائل خطأ في Console؟**
   - نعم → ابعث لي الرسائل
   - لا → المشكلة قد تكون في الشبكة

3. **هل البيانات في `.env.local` صحيحة؟**
   - تأكد من https://cloudinary.com/console

4. **هل الصورة التي ترفعها صالحة؟**
   - نوع: JPG/PNG/WebP
   - حجم: أقل من 10 MB
   - ليست تالفة

## 🆘 إذا لم ينجح شيء

ابعث لي:
1. لقطة شاشة من Browser Console (F12)
2. الرسالة الكاملة من الخطأ
3. نوع وحجم الملف الذي تحاول رفعه
4. نتيجة تشغيل: `.\test-upload.ps1`

---

**آخر تحديث:** 26 ديسمبر 2025
