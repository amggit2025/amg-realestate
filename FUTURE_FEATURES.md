# 🚀 الميزات المستقبلية - AMG Real Estate

هذا الملف يحتوي على الأفكار والميزات المخطط تنفيذها لاحقاً.

---

## 📋 قائمة الميزات

| # | الميزة | الأولوية | الحالة |
|---|--------|----------|--------|
| 1 | اعرض عقارك للتسويق | 🔴 عالية | ⏳ قيد التخطيط |

---

## 1️⃣ اعرض عقارك للتسويق

### 📝 وصف الفكرة
صفحة مخصصة لأصحاب العقارات الذين يريدون عرض عقاراتهم للبيع أو الإيجار من خلال شركة AMG.
الشخص يملأ تفاصيل عقاره ويرفع صور ويطلب من الشركة تسوق له وتنزل معاينة.

### 🎯 الهدف
- استقطاب عملاء جدد (أصحاب عقارات)
- زيادة محفظة العقارات المتاحة للتسويق
- تقديم خدمة تسويق احترافية

### 👥 المستخدم المستهدف
- شخص يملك عقار (شقة، فيلا، أرض، محل، مكتب)
- يريد بيعه أو تأجيره
- يحتاج مساعدة في التسويق والبيع

### 🔄 سير العمل (Flow)

```
1. الزائر يدخل صفحة "اعرض عقارك للتسويق"
   ↓
2. يملأ نموذج بيانات العقار:
   - نوع العقار (شقة، فيلا، أرض، محل...)
   - الغرض (بيع / إيجار)
   - المساحة
   - السعر المطلوب
   - الموقع (المحافظة، المدينة، المنطقة)
   - عدد الغرف والحمامات
   - وصف تفصيلي
   - المميزات
   ↓
3. يرفع صور العقار (5-10 صور)
   ↓
4. يملأ بيانات التواصل:
   - الاسم
   - رقم الهاتف
   - البريد الإلكتروني
   - الوقت المفضل للتواصل
   ↓
5. يختار الخدمة المطلوبة:
   □ تسويق العقار فقط
   □ تسويق + معاينة من الشركة
   □ تقييم سعر العقار
   ↓
6. يرسل الطلب
   ↓
7. يظهر رسالة نجاح + رقم الطلب
   ↓
8. إيميل تأكيد للعميل
   ↓
9. إشعار للأدمن بالطلب الجديد
```

### 📐 التصميم المقترح

#### الصفحة الرئيسية `/list-your-property`
```
┌─────────────────────────────────────────────────────┐
│  🏠 اعرض عقارك للتسويق مع AMG                      │
│  ────────────────────────────────────               │
│  نساعدك في بيع أو تأجير عقارك بأفضل سعر            │
│                                                     │
│  ✅ تسويق احترافي    ✅ تصوير عقاري                │
│  ✅ تقييم مجاني      ✅ فريق مبيعات متخصص          │
│                                                     │
│  [────────────── Progress Bar ──────────────]       │
│       1. العقار → 2. الصور → 3. التواصل → 4. تأكيد  │
│                                                     │
│  ┌──────────────────────────────────────────┐      │
│  │           نموذج بيانات العقار            │      │
│  │  ○ نوع العقار    ○ المساحة    ○ السعر   │      │
│  │  ○ الموقع        ○ الغرف      ○ الوصف   │      │
│  └──────────────────────────────────────────┘      │
│                                                     │
│            [ التالي ← ]                             │
└─────────────────────────────────────────────────────┘
```

### 🗃️ قاعدة البيانات

#### جدول جديد: `PropertyListingRequest`
```prisma
model PropertyListingRequest {
  id              String   @id @default(cuid())
  
  // بيانات العقار
  propertyType    String   // APARTMENT, VILLA, LAND, OFFICE, COMMERCIAL
  purpose         String   // SALE, RENT
  area            Float
  price           Float
  currency        String   @default("EGP")
  
  // الموقع
  governorate     String
  city            String
  district        String
  address         String?
  
  // التفاصيل
  bedrooms        Int?
  bathrooms       Int?
  floors          Int?
  floor           Int?
  yearBuilt       Int?
  features        String?  @db.Text  // JSON array
  description     String   @db.Text
  
  // الصور
  images          String   @db.Text  // JSON array of URLs
  
  // بيانات التواصل
  ownerName       String
  ownerPhone      String
  ownerEmail      String
  preferredTime   String?  // صباحاً، ظهراً، مساءً
  
  // الخدمة المطلوبة
  serviceType     String   // MARKETING_ONLY, MARKETING_AND_VISIT, VALUATION
  
  // الحالة
  status          String   @default("PENDING") // PENDING, REVIEWING, APPROVED, REJECTED, COMPLETED
  adminNotes      String?  @db.Text
  assignedTo      String?  // Admin ID
  
  // التواريخ
  visitDate       DateTime?
  visitNotes      String?  @db.Text
  
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  @@index([status])
  @@index([propertyType])
  @@index([createdAt])
  @@map("property_listing_requests")
}
```

### 🔌 API Endpoints

```
POST   /api/listing-requests          - إنشاء طلب جديد
GET    /api/listing-requests          - جلب الطلبات (للأدمن)
GET    /api/listing-requests/[id]     - تفاصيل طلب
PATCH  /api/listing-requests/[id]     - تحديث الحالة
DELETE /api/listing-requests/[id]     - حذف طلب
POST   /api/listing-requests/upload   - رفع الصور
```

### 📱 الصفحات المطلوبة

1. **صفحة الزائر**: `/list-your-property`
   - نموذج متعدد الخطوات
   - رفع صور
   - رسالة نجاح

2. **لوحة الأدمن**: `/admin/listing-requests`
   - قائمة الطلبات
   - فلترة حسب الحالة
   - عرض التفاصيل
   - تغيير الحالة
   - جدولة معاينة
   - إضافة ملاحظات

### 📧 الإيميلات

1. **للعميل عند إرسال الطلب**:
   - تأكيد استلام الطلب
   - رقم الطلب
   - الخطوات القادمة

2. **للأدمن عند وصول طلب جديد**:
   - تفاصيل العقار
   - بيانات المالك
   - رابط للوحة التحكم

3. **للعميل عند تغيير الحالة**:
   - إشعار بالموافقة/الرفض
   - موعد المعاينة

### 🎨 مكان الزر في الموقع

1. **Header** - بجانب "حجز معاينة":
   ```
   [ حجز معاينة 📅 ] [ اعرض عقارك 🏠 ] [ تسجيل دخول ]
   ```

2. **الصفحة الرئيسية** - قسم خاص:
   ```
   ┌─────────────────────────────────────────┐
   │  🏠 هل تملك عقاراً للبيع أو الإيجار؟   │
   │  دعنا نساعدك في تسويقه وبيعه بأفضل سعر │
   │         [ اعرض عقارك الآن ← ]          │
   └─────────────────────────────────────────┘
   ```

3. **Footer** - في قسم الروابط السريعة

### 🔧 المتطلبات التقنية

- [ ] Prisma model للطلبات
- [ ] API endpoints (CRUD)
- [ ] رفع صور Cloudinary
- [ ] صفحة النموذج متعددة الخطوات
- [ ] Validation كامل
- [ ] إيميلات (3 قوالب)
- [ ] لوحة أدمن للطلبات
- [ ] إحصائيات في Dashboard

### ⏱️ الوقت المتوقع
- التصميم: 1 ساعة
- Backend (Model + API): 2 ساعة
- Frontend (صفحة النموذج): 3 ساعات
- Admin Panel: 2 ساعة
- الإيميلات: 1 ساعة
- **المجموع**: ~9 ساعات

### 📌 ملاحظات إضافية
- يمكن ربطها بنظام الـ CRM لاحقاً
- إضافة تتبع حالة الطلب للعميل
- إمكانية تحويل الطلب لعقار منشور بعد الموافقة

---

## 📝 كيفية إضافة ميزة جديدة

1. أضف الميزة في الجدول أعلاه
2. أنشئ قسم جديد بالتفاصيل الكاملة
3. حدد الأولوية والوقت المتوقع
4. عند البدء، غيّر الحالة إلى "🔄 قيد التنفيذ"
5. عند الانتهاء، غيّر إلى "✅ مكتمل"

---

*آخر تحديث: 25 ديسمبر 2025*
