# 🎨 First Visit Loader - شاشة الترحيب للزائر الجديد

## 📋 كيف يعمل النظام:

### الزيارة الأولى:
1. **المستخدم يفتح الموقع لأول مرة** ✨
2. يظهر شاشة Loading جميلة مع:
   - أنيميشن بناء المنزل 🏠
   - شعار AMG
   - رسالة ترحيب
   - مدة العرض: 3 ثواني

3. **يتم حفظ في localStorage:**
```javascript
localStorage.setItem('amg-visited', 'true')
```

### الزيارات التالية:
- ❌ **لا تظهر** شاشة Loading الرئيسية
- ✅ فقط loading داخلي عادي للصفحات

## 🔧 التحكم في النظام:

### تغيير مدة العرض:
في ملف `FirstVisitLoader.tsx`:
```javascript
const timer = setTimeout(() => {
  setIsLoading(false)
}, 3000) // غير 3000 إلى المدة المطلوبة بالميلي ثانية
```

### إعادة ضبط (للاختبار):
افتح Console في المتصفح واكتب:
```javascript
localStorage.removeItem('amg-visited')
```
ثم refresh الصفحة - هتشوف شاشة Loading مرة أخرى

### تعطيل الميزة مؤقتاً:
في `layout.tsx`، علق السطر:
```tsx
// <FirstVisitLoader />
```

## 📂 الملفات المعدلة:

1. `src/components/layout/FirstVisitLoader.tsx` - Component جديد
2. `src/app/layout.tsx` - إضافة الـ component

## ✨ الميزات:

- 🎯 يظهر **مرة واحدة فقط** للزائر الجديد
- 💾 يستخدم **localStorage** (يعمل offline)
- 🎨 تصميم **modern** مع أنيميشن smooth
- ⚡ **لا يؤثر** على الأداء في الزيارات التالية
- 🔄 **Exit animation** سلس

## 🧪 كيف تختبر:

1. افتح الموقع في **Incognito/Private mode**
2. ستشاهد شاشة Loading الجميلة ✨
3. أغلق وافتح مرة أخرى في نفس النافذة
4. لن تظهر شاشة Loading مرة أخرى ✅

---

**ملاحظة:** الملف مش مرفوع على Git عشان تقدر تعدل عليه local! 🎉
